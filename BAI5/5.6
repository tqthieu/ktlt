# main.py
import my_module6

def main():
    # Nhập số lượng phần tử
    n = int(input("Nhập số lượng phần tử trong danh sách: "))
    
    # Nhập giá trị danh sách
    lst = []
    for i in range(n):
        value = float(input(f"Nhập phần tử thứ {i + 1}: "))
        lst.append(value)

    # Sử dụng module để sắp xếp, tìm giá trị lớn nhất và nhỏ nhất
    sorted_list = my_module.sort_list(lst)
    max_value = my_module.find_max(lst)
    min_value = my_module.find_min(lst)

    # Tìm vị trí của phần tử lớn nhất và nhỏ nhất
    max_index = lst.index(max_value) + 1  # Thêm 1 để hiển thị vị trí theo cách đếm từ 1
    min_index = lst.index(min_value) + 1

    print("\nKết quả:")
    print(f"Danh sách đã sắp xếp: {sorted_list}")
    print(f"Phần tử lớn nhất: {max_value} (Vị trí: {max_index})")
    print(f"Phần tử nhỏ nhất: {min_value} (Vị trí: {min_index})")

if __name__ == "__main__":
    main()
